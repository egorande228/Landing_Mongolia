Optional non-runtime reference images can be placed in this folder and linked from
`content/mongolia-image-slots.ts` through `referenceImagePaths`.

They are only used by `npm run generate:images` when the files exist.
